# Qwen3.6-35B-A3B-4bit 最佳模型设置

> **生成平台**：OMLX 0.3.10  
> **运行硬件**：Mac mini 4 M4（内存 64GB 统一内存，SSD 2TB）  
> **使用场景**：Java 后端开发 / 前端开发 / 小程序开发 / 代码重构

---

## 1. 模型基本信息

| 属性 | 值 |
| :--- | :--- |
| **模型名称** | Qwen3.6-35B-A3B-4bit |
| **模型架构类型** | **混合专家模型 (MoE, Mixture of Experts)** |
| **总参数量** | 35B（全部专家权重常驻统一内存） |
| **活跃参数量** | 约 3B（A3B = Active 3B，每次 Token 仅激活 3B 参数） |
| **4bit 量化后内存占用** | 约 20~22 GB |
| **架构说明** | MoE（混合专家）架构的核心优势在于：总参数 35B 提供了大模型的表达深度和逻辑广度，而每次推理仅激活约 3B 参数实现极低的计算开销。这使得它在 **逻辑能力接近 35B 稠密模型** 的同时，**计算速度接近 3B 稠密模型**。但由于全部专家权重仍常驻内存，它对总内存容量的要求等同于 35B 模型级别。 |

---

## 2. 推理加速技术深度解析

### 2.1 DFlash 块扩散投机解码 — 推荐：✅ **开启**

| 项目 | 说明 |
| :--- | :--- |
| **为什么需要** | 尽管 MoE 模型仅激活 3B 参数使得计算负载极低，但在本地单请求模式下，解码速度的瓶颈仍然是统一内存带宽（需要从内存读取全部 35B 权重）。DFlash 通过引入专为 35B MoE 架构设计的草稿模型，并行预测多 Token 并由主模型验证，可以显著突破内存带宽瓶颈。 |
| **核心优点** | ① **3~4x 解码速度提升**，让 35B 级模型获得接近 7B 模型的 Token 输出速度；② **无损精度**，主模型验证保证代码生成的逻辑深度和正确性；③ **自动降级保护**，超长上下文自动回退标准解码。 |
| **核心缺点** | ① 需额外加载 MoE 专用的 DFlash 草稿模型（约 3~5 GB 内存），但在 64GB 内存上完全可行；② 草稿模型上下文窗口有限，超过 4096 Token 自动降级；③ 仅支持单请求流。 |
| **Draft 模型推荐** | `z-lab/Qwen3.6-35B-A3B-DFlash` — 官方提供的 DFlash 草稿模型，专为 Qwen 3.6 35B A3B MoE 架构匹配，可直接从 HuggingFace 下载。 |

### 2.2 SpecPrefill 投机预填充 — 推荐：❌ **关闭**

| 项目 | 说明 |
| :--- | :--- |
| **原理** | 用极小的草稿模型加速超长 Prompt 的预填充阶段，降低首字延迟。 |
| **为何关闭** | ① 与 DFlash 存在功能重叠且会相互冲突；② 短 Prompt 下无加速效果；③ MoE 模型本身计算预填充速度已经很快，SpecPrefill 的额外收益极有限。开启 DFlash 后此项应保持关闭。 |

### 2.3 Native MTP 原生多 Token 预测 — 推荐：❌ **关闭（当前不可用）**

| 项目 | 说明 |
| :--- | :--- |
| **原理** | 利用模型内置 MTP 预测头一次性预测多个未来 Token，无需草稿模型，约 1.5x 提速。 |
| **当前状态** | **不可用**。当前 4bit 量化版转换时遗失了 `mtp.*` 权重张量，OMLX 加载会报 `Config declares MTP layers but the converted weights are missing mtp.* tensors` 错误。 |
| **启用指南** | ① 下载带有 MTP 头的官方 HF 原生仓库（关注 Qwen 官方发布的带 `-MTP` 后缀版本）；② 使用最新合并 PR #990 和 PR #15 的 `mlx-lm` 重新量化转换。 |

---

## 3. OMLX 0.3.10 模型设置参数列表

### 4.1 模型类别
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **模型类别** | `llm` | 强制指定为纯文本大语言模型，避免自动检测误判为 VLM 或其他类别，确保工具调用稳定。 |

### 4.2 REASONING PARSER
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **REASONING PARSER** | `qwen_3_5` | Qwen 3.6 MoE 架构选用 `qwen_3_5` 解析器（根据文档说明，`qwen_3_5` 和 `qwen_3_coder` 均适用于 Qwen 3.5/3.6，MoE 架构更推荐 `qwen_3_5`），可正确截取模型的推理链输出结构。 |

### 4.3 ~ 4.11 采样与输出参数
| 配置项 | 推荐设定值 | 针对防假死、防死循环与输出质量的设计理由说明 |
| :--- | :--- | :--- |
| **4.3 Ctx Window** | `32768` | 64GB 统一内存充裕。32K Token 上下文足够支撑大型 Java 项目的复杂调用链分析与跨文件重构。MoE 模型在长上下文中依然保持较低的计算开销。 |
| **4.4 最大 Token 数** | `4096` | 限制单次输出长度，防止模型发散导致无限输出或假死。代码生成场景下 4096 Token 足够覆盖大部分方法级和类级的输出。 |
| **4.5 温度** | `0.2` | 低温度确保代码生成的高确定性，杜绝胡言乱语。 |
| **4.6 Top P** | `0.9` | 剔除长尾低概率 Token，稳定输出结构。 |
| **4.7 Top K** | `40` | 限制采样池大小，增加代码输出的严谨性。 |
| **4.8 Min P** | `0.05` | **防假死核心参数**。动态剔除概率低于最大概率 5% 的 Token，防止模型产生突发性胡言乱语或陷入复读假死。 |
| **4.9 重复惩罚** | `1.05` | **防死循环核心参数**。微调至 1.05 可强力打破"无限自我复读"的死循环，同时不破坏代码中正常的括号和变量重复。 |
| **4.10 Presence Penalty** | `0.0` | 保持默认，防止改变正常代码词汇的出现倾向，确保代码输出的原生态。 |
| **4.11 TTL（秒）** | `7200` | 维持激活状态 2 小时。作为可选主力模型，避免频繁重新加载（35B MoE 加载时间较长），保证开发连续性。 |

### 4.12 ~ 4.14 思考与工具参数
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **4.12 启用思考** | `no` | Qwen 3.6 非 R1 推理模型，关闭思考可以成倍提升 Token 输出速度，避免生成无价值的内部推理链浪费 Token 和时间。 |
| **4.13 思考 Token 限制** | `no` | 配合 4.12 关闭思考一同关闭，无需设置。 |
| **4.14 限制工具结果 Token** | `no` | **⚠️ 关键避坑配置**。强烈建议设为 **`no`**。开启后会导致 Roo Code / Cline 读取的代码文件被截断，引发工具返回的 JSON/XML 格式破损，模型解析失败后不断重试，陷入"工具调用 → 截断 → 解析失败 → 重试"的无限死循环。64GB 内存下关闭此选项安全无虞。 |

### 4.15 ~ 4.16 运行时配置
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **4.15 强制采样** | `no` | 允许前端工具按需动态覆盖温度和 Top P 参数，保持灵活调整空间。 |
| **4.16 Trust Remote Code** | `yes` | MLX 模型加载需要运行自定义 modeling 代码，设为 `yes` 保证模型能顺利初始化。 |

### 4.17 聊天模板参数
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **enable_thinking** | `false`（不强制） | 不强制约束。默认关闭思考链，交给前端工具按需控制。 |
| **reasoning_effort** | `medium`（不强制） | 不强制约束。中等推理努力度在代码生成质量与速度间取得良好平衡。 |
| **自定义模板** | 暂不添加 | 如需添加自定义参数（如特殊 stop token），可在下方按需提供键值对。 |

### 4.18 TurboQuant KV Cache
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **TurboQuant KV Cache** | `yes` | 开启 KV 缓存量化，大幅降低多轮对话的内存消耗，提升缓存读写速度。 |
| **Bits per channel** | `4-bit` | 4-bit 是 MoE 模型 KV 量化的最佳选择，在节省约 75% KV 缓存内存的同时保持足够的注意力精度。 |

### 4.19 SpecPrefill
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **SpecPrefill** | `no` | 已启用 DFlash，SpecPrefill 与其功能重叠且冲突，必须关闭。 |

### 4.20 DFlash 块扩散投机解码（全部子项）

#### 4.20 DFlash 主开关
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **DFlash** | `yes` | **开启**。MoE 模型受益于 DFlash 的并行验证机制，可实现 3~4x 的无损解码速度提升。 |

#### 4.20.1 Draft Model
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **Draft Model** | `z-lab/Qwen3.6-35B-A3B-DFlash` | ✅ **真实存在的官方 DFlash 草稿模型**。仓库地址：`https://huggingface.co/z-lab/Qwen3.6-35B-A3B-DFlash`，可直接下载使用。该草稿模型专为 Qwen 3.6 35B A3B MoE 设计，验证匹配率高。 |

#### 4.20.2 Quantization（草稿模型量化）
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **Quantization** | `no` | 不开启量化。64GB 内存充足，保留 Draft 模型全精度权重可最大化 Token 匹配率，生成速度最快。 |

#### 4.20.3 Max Context
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **Max Context** | `4096` | 草稿模型上下文限制。超过此阈值自动回退到标准解码模式，防止 DFlash 退化。 |

#### 4.20.4 Long-context tuning
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **Draft window size** | `1024` | 草稿模型滑动注意力窗口，默认值稳定可靠。 |
| **Draft sink size** | `64` | 注意力汇聚 Token，保持首部 64 个 Token 的全局注意力。 |

#### 4.20.5 Verify mode
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **Verify mode** | `adaptive` | 自适应验证模式，引擎自动选择最优验证策略。 |

#### 4.20.6 In-memory cache
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **In-memory cache** | `yes` | 启用前缀快照缓存，加速多轮对话速度。 |
| **Max entries** | `8` | 缓存 8 个对话前缀快照。 |
| **Size (GiB)** | `4.0` | 分配 4GB L1 缓存预算。 |

#### 4.20.7 SSD cache size
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **SSD cache size** | `no` | 64GB 内存充裕，无需 SSD 溢出缓存。 |

### 4.21 Native MTP
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **Native MTP** | `no` | 当前量化版本缺少 `mtp.*` 张量，开启会报错。关闭以防止错误。 |

### 4.22 VLM MTP
| 配置项 | 推荐设定值 | 设计理由 |
| :--- | :--- | :--- |
| **VLM MTP** | `no` | 仅限 Gemma 4 视觉模型使用，Qwen 3.6 文本模型不适用，必须关闭。 |

---

## 4. 前端调用工具 (Roo Code / Cline) 专用系统提示词

```markdown
# 智能体运行准则（防止假死与工具死循环）

## 1. 工具调用截断保护
- 当你读取文件或获取工具输出时，若发现内容不完整、缺少结尾括号、JSON/XML 结构错乱、或包含 [TRUNCATED] 字样，这表明工具返回结果受到了限制。
- **绝对禁止** 在不作任何修改的情况下连续重复调用同一个读取工具。
- 解决方案：必须改用分段读取命令（如指定行范围），或修改检索逻辑只读取特定区域，或直接告知用户文件过长需要人工介入。

## 2. 自我复读阻断机制
- 在任何情况下，如果你发现你的输出中包含与上一个回复高度重合的重复文本（自我复读/无限循环现象），必须立刻停止当前生成。
- 立即采取以下任意手段打破循环：① 换用完全不同的措辞重新总结问题；② 向用户提出一个明确的具体问题以打断循环；③ 调用一个不同的工具（如搜索而非读取）改变输出模式。

## 3. 工具调用失败恢复策略
- 当工具调用返回错误时，最多重试 1 次。如果再次失败，必须换用替代方案（如改用其他工具、询问用户、或输出明确的错误说明）。
- **绝对禁止** 连续 3 次以上调用同一个失败的工具而不改变策略。

## 4. 代码设计与重构标准
- **Java 开发**：严格遵循 Spring Boot 最新规范及阿里巴巴 Java 开发规约；分层清晰（Controller / Service / Repository）；异常处理禁止吞掉堆栈信息。
- **前端开发**：遵循组件化规范和单向数据流（如 React/Vue 最佳实践）；避免大型组件，合理划分职责边界。
- **小程序开发**：注意内存占用与性能瓶颈，避免一次性加载大量数据，合理使用分页和虚拟列表。
- **架构能力**：所有重构动作必须保证向下兼容；在重构核心模块前，先梳理调用链，并在代码中清晰注释核心逻辑和变更原因。
- **编码规范**：命名清晰（类名大驼峰、方法/变量小驼峰、常量全大写下划线）；工具类、接口类必须定义明确的职责边界，禁止出现上帝类（God Class）。
```

---

## 5. 配置总结与注意事项

| 关注点 | 配置策略 |
| :--- | :--- |
| **防假死 / 防死循环** | Min P=0.05 + 重复惩罚=1.05 + 限制工具结果Token=no + 关闭思考 |
| **防内存溢出** | Ctx Window=32K + TurboQuant KV=4-bit + DFlash Max Context=4096 降级保护 |
| **Token 输出速度** | DFlash 开启 + SpecPrefill 关闭 + 思考关闭 |
| **工具调用稳定性** | 限制工具结果Token=no（最关键！）+ 前端提示词中的失败恢复策略 |
| **模型准确性** | 温度=0.2 + 强制采样=no + Trust Remote Code=yes |
| **内存预算** | 主模型 ~22GB + Draft ~4GB + KV 缓存 ~2GB + L1 缓存 4GB = 约 32GB，64GB 总内存绰绰有余 |

> **⚠️ 特别提醒**：Qwen 3.6-35B-A3B MoE 模型与 27B 稠密模型相比，在逻辑推理和长代码生成质量上略有优势，但激活参数量更小意味着计算延迟更低。如果同时激活两个 Qwen 模型，建议以 27B 稠密为主力，35B MoE 为备选。
